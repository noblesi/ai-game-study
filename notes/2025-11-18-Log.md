## 2025-11-18

### 오늘 한 것
- python-basics.ipynb에서 유닛 데이터를 리스트/딕셔너리 구조로 정의
- 개별/전체 유닛의 정보를 출력하는 함수 구현  
  - print_unit_info, print_unit_info_by_name, print_all_units
- 유닛의 스탯을 계산/조작하는 함수 구현  
  - 평균 공격력 계산: get_average_atk  
  - 최대 HP 유닛 찾기: get_max_hp_unit  
  - 단일/전체 레벨업: level_up, level_up_all
- 유닛 데이터를 최초 상태로 되돌리는 리셋 함수 구현  
  - 개별 리셋: reset_unit  
  - 전체 리셋: reset_all_units  
  - copy.deepcopy + dict.clear() + dict.update() 패턴으로 원본(initial_units) 유지

### 배운/느낀 점
- 하나의 셀에 전부 코드를 넣기보다는,  
  - “초기 데이터 셀 / 함수 정의 셀 / 테스트 셀”로 나누는 편이 디버깅과 재실행에 훨씬 유리하다는 점을 느낌.
- 파이썬에서 dict는 참조로 공유되기 때문에,  
  - clear() 후 update()로 내용을 갈아끼우면 같은 객체를 유지한 채 값만 초기 상태로 되돌릴 수 있다는 걸 이해함.
- 게임에서 자주 쓰는 개념(유닛, 스탯, 레벨업, 리셋)을 파이썬으로 구현해보니  
  - 이후 AI/ML을 붙일 때도 “데이터 구조 → 통계/분석 → 조작” 흐름을 자연스럽게 가져갈 수 있을 것 같다는 감각을 얻음.

### 내일/다음에 할 것
- 현재 dict 기반 유닛 로직을 Unit 클래스로 옮겨보면서 __init__, 인스턴스 메서드 연습해보기  
  - ex) level_up, reset, print_info를 클래스 메서드로 구현
- numpy-matrix.ipynb를 만들어서 2D 좌표, 벡터/행렬, 회전 변환, 간단한 시각화(matplotlib) 연습 시작
- 오늘 만든 함수들을 조금 더 정리해서, 나중에 .py 모듈로 분리할 수 있는 구조를 염두에 두기
