## 2026-01-02 AI, 파이썬 데일리 로그 (Defense Balancing Tool)

### 오늘 한 것
- PowerShell 실행 정책 이슈 대응(현재 세션만 우회)
  - `Set-ExecutionPolicy -Scope Process Bypass`
- 의존성 점검/설치
  - `pip install -r requirements.txt`로 `matplotlib` 누락 재발 방지
- 주간 파이프라인 흐름 안정화 점검
  - `weekly_run.ps1` / `run_pipeline.ps1` / `promote_models.ps1` 위치 확인(루트/ scripts 양쪽 고려)
- current 모델 누락으로 인한 포트폴리오팩 실패 방지
  - `models/rf_full_current.joblib` 등 current 모델 3종 존재 여부 확인
  - 누락 시 `promote_models.ps1 -Tag 20260102`로 current 모델 생성/승격
- 원클릭 실행 루틴 정리
  - `.\weekly_run.ps1 -Tag 20260102 -ForcePromote`
  - 산출물: `reports/portfolio_YYYYMMDD/` (md/csv/png) 생성 확인

### 어려웠던 점 / 체크포인트
- (체크) PS 실행 정책 때문에 ps1이 바로 막힘 → `Process` 스코프로만 우회하면 시스템 설정 건드리지 않고 해결
- (체크) `matplotlib` 미설치 시 `make_portfolio_pack.py`에서 즉시 실패 → requirements에 포함/설치 선행 필요
- (핵심) 포트폴리오팩은 `portfolio_runs_current.json`이 가리키는 current 모델 파일이 실제로 있어야 함
  - `models/rf_full_current.joblib` 누락 → `FileNotFoundError` 재발 가능
  - 해결: 포트폴리오팩 전에 승격/생성 단계가 선행되도록 흐름 고정
- (체크) 스크립트를 어디서 실행하느냐에 따라 `run_pipeline.ps1 not found`가 뜰 수 있음
  - 해결: 루트/`scripts/` 경로를 모두 탐색하도록 스크립트 보강(또는 항상 루트에서 실행)
