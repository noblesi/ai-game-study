## 2025-11-22 파이썬 공부 로그

### 오늘 한 것
- if / while 문으로 간단한 워밍업 문제 3개 풀이
  - 나이에 따라 어린이/청소년/성인 분기 처리
  - while로 1부터 10까지 합 계산
  - 몬스터 HP를 깎으면서 "몬스터 처치!"까지 출력하는 전투 루프 구현
- 함수 기초 문법 학습
  - 인자 없는 함수, 인자 있는 함수, 반환값이 있는 함수 연습
  - `def`, 매개변수, `return`의 역할 정리
- 기존 텍스트 전투 루프 리팩터링
  - `print_unit_status()`, `player_turn()`, `monster_turn()`, `battle_loop()` 등으로 기능 분리
  - 전투 흐름을 `battle_loop()` 하나의 while에서 관리하도록 구조 개선
- 리스트 & 수학 관련 문법 복습
  - `del`, `remove()`, `pop()`으로 리스트에서 몬스터 삭제 연습
  - `**` 연산자로 거듭제곱, `math.sqrt()`로 제곱근 계산
  - `실제 공격력 = atk * (1.1 ** level)` 공식을 코드로 작성해봄

### 어려웠던 점
- 전투 로직을 함수로 쪼갤 때 어떤 책임을 어떤 함수에 넣어야 할지 헷갈렸음
- while 루프 종료 조건을
  - 몬스터 사망
  - 플레이어 사망
  - 도망 선택
  이렇게 모두 고려해서 설계하는 부분이 생각보다 복잡하게 느껴졌음
- 실수(소수점)로 계산된 공격력을 출력할 때 자리수를 어떻게 맞출지 고민함  
  → f-string과 `:.1f` 같은 포맷을 더 찾아볼 필요성을 느낌