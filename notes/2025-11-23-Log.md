## 2025-11-23 파이썬 공부 로그

### 오늘 한 것
- 콘솔 기반 유닛 관리 프로그램 구현
  - 유닛 목록 출력 함수 `print_units` 작성
  - 유닛 추가 메뉴 함수 `add_unit_menu`에서 `input()`과 `strip()`으로 입력 처리
  - 레벨 업 기능을 담당하는 `level_up_unit` 함수 구현
  - 유닛 삭제 기능을 담당하는 `remove_unit_menu` 함수 구현
- `while True` + `if / elif / else`로 메인 메뉴 루프(`main_loop`) 구성
- 잘못된 입력에 대비해 `try / except ValueError`로 숫자 입력 예외 처리

### 어려웠던 점 / 헷갈렸던 점
- 이름이 잘못 입력되었을 때(공백 입력, 존재하지 않는 이름 등) 어떻게 처리할지 조건을 나누는 부분이 조금 헷갈렸음
- 유닛이 하나도 없을 때와 있을 때의 출력을 분리하는 로직을 짜는 과정에서 분기 처리를 여러 번 확인함