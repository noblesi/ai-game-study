## 2025-12-25 AI, 파이썬 데일리 로그

### 오늘 한 것
- perk 확장 작업을 진행하면서 발생한 에러를 재현하고 원인 파악
  - `train_baseline.py` 실행 시 `to_float` 미정의(NameError) 문제 확인
  - `batch_experiments.py`에서 유닛 복제 시 `Unit` 미정의 오류 발생 포인트 확인
  - `perk_io.py`가 프로젝트에 없거나/혼재되어 import가 깨지는 문제 확인 → `perk.py(common/perk.py)`로 합치기 방향 확정
- 공용 유틸(common) 구조 점검
  - `cli.py / jsonl.py / parse.py / perk.py`를 `defense_balancing_tool/common`에 두는 구조로 정리
  - 다만 기존 코드에서 `from common.xxx import ...` 형태로 import하고 있어 경로(패키지) 정합성 이슈 확인
- perk 확장 파이프라인(배치 실험 → 데이터셋 추출 → 학습)에서 꼬이는 지점들을 목록화
  - `select_from_list` 시그니처와 호출부 불일치 가능성 확인
  - `unit_logic.py`가 `load_perk_catalog()`를 요구하는 구조라 common/perk에 SSOT 필요

### 어려웠던 점 / 체크포인트
- 공용 모듈 경로가 `common/`(루트)인지 `defense_balancing_tool/common/`인지에 따라 import가 전체적으로 깨질 수 있음
- perk 로딩 함수가 `perk_io.py`와 `perk.py`로 분산되어 있으면 실험/추출/학습 단계에서 서로 다른 카탈로그를 보게 되어 스키마가 흔들림
- `select_from_list(items, render)` vs `select_from_list(..., title=, render_item=)` 형태 불일치는 런타임 오류/출력 이상으로 이어질 수 있음

### 다음 작업(우선순위)
1) `common/perk.py`에 `load_perk_catalog()`를 SSOT로 확정하고 `perk_io.py`는 래퍼 또는 제거
2) 프로젝트 경로 기준 import 규칙 통일
   - 루트 `common/`로 둘지, `defense_balancing_tool.common`로 둘지 한 가지로 고정
3) `cli.select_from_list` 시그니처와 `unit_logic.py` 호출부를 한 방식으로 통일
4) perk 컬럼이 `export_dataset.py → train_baseline.py`까지 안정적으로 흘러가도록 (a_has_perk_*, d_has_perk_*) 스키마 고정
