# 2025-12-28 데일리 로그 (Defense Balancing Tool)

## 오늘 한 것
- `train_baseline.py`의 **FIXED_TEST threshold 진단/조정 기능**을 실제로 동작하도록 패치
  - `--fixed-thresholds`로 지정 threshold 리스트 성능을 콘솔에 출력(`[FIXED_THR]`)
  - `--fixed-threshold-sweep` + `--fixed-threshold-metric`로 FIXED_TEST에서 sweep 후 상위 N개 출력(`[FIXED_SWEEP]`)
  - sweep 결과 1등 threshold를 FIXED_TEST 평가에 적용되도록 연결
- 실행 결과 확인
  - best model: `LR_balanced` 유지
  - FIXED_TEST(usable 12, overlap group 27 drop) 기준
    - 기본 thr=0.50 → `f1_1=0.5000 / recall_1=0.3333 / prec_1=1.0000`
    - fixed thresholds(0.20~0.30) → `f1_1=0.6667 / recall_1=0.6667 / prec_1=0.6667`
    - FIXED_SWEEP(metric=recall_1) 최상위 thr=0.10 적용 → `recall_1=1.0000 / f1_1=0.6667 / bal_acc=0.8333`
- 리포트 생성 확인
  - `reports/baseline_eval_20251228_group_pair_key.md`
  - `reports/baseline_eval_20251228_group_pair_key_fixedtest.md`

## 어려웠던 점 / 체크포인트
- `[WARN] threshold tuning skipped (predict_proba not available).`
  - split 기반 튜닝(`--tune-threshold`)은 **predict_proba 경로가 막혀 있어서** 스킵됨
  - 대신 FIXED_TEST에서는 proba 기반 sweep이 정상 동작하므로,
    - “운영용 threshold는 (tuned) 대신 fixed_sweep 결과/정책으로 정하는 흐름”을 고려해야 함
- FIXED_TEST usable rows가 12로 작아서(thr=0.10 vs 0.20의 차이가 FP 몇 개로 갈림)
  - 이 수치는 **정책 실험**에는 유용하지만, 확정 결론으로 쓰기엔 샘플이 작음
  - 다음 액션: 고정 테스트 시나리오를 더 늘리거나(특히 unseen pair), drop-overlap 이후에도 충분한 샘플 확보
