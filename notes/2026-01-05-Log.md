# 2026-01-05 AI, 파이썬 데일리 로그

## 오늘 한 것
- `weekly_run.ps1` → `run_pipeline.ps1` 호출 방식 수정
  - 기존: 파라미터 없이 실행(점 소싱/직접 호출로 스코프/인자 전달 꼬임)
  - 변경: `call operator(&)` + **splatting(`@pipeArgs`)**로 파라미터를 안전하게 전달
  - 결과: `Seeds=0` 문제 해결 → STEP3 실험 생성 정상 수행
- 파이프라인 전체 정상 실행 확인
  - scenario 39개 생성/검증 OK
  - experiments 2400개 생성/검증 OK
  - `train_experiments_v1.csv` 2400 rows 생성 OK
  - baseline 학습/리포트/모델 저장 OK
- 주간 러너 전체 플로우 정상 동작 확인
  - FULL / NO_PERK / NO_ADV 3종 학습 리포트 생성
  - 기준 충족으로 모델 승격(promote) 완료 (tag=20260105)
  - `make_portfolio_pack.py` 실행 성공 → `reports/portfolio_20260105`
  - `make_weekly_portfolio.py` 실행 성공 → `reports/portfolio_week_20260105`

## 어려웠던 점 / 체크포인트
- PowerShell에서 스크립트 호출 시 **dot-sourcing(`.`)** 과 **백틱 인자 나열**이 섞이면
  - 파라미터 바인딩/스코프가 꼬여 `Seeds=0` 또는 `Seeds 인수에 "-Seeds"가 들어가는` 현상이 발생할 수 있음
- 해결책 체크포인트
  - 스크립트 실행은 `& script.ps1` 형태로 통일 (dot-sourcing 지양)
  - 파라미터 전달은 문자열 조립 대신 `@{...}` splatting으로 안전하게 전달
  - STEP3 전후로 데이터 생성 여부 가드(실험 0개면 즉시 중단) 유지
